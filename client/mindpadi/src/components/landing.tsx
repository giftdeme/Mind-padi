import React from 'react'
import brand from './brand'

const landing = () => {
  
  return (
    <div>
      <ul>{
        brand.map(())
        }</ul>
      
    </div>
  )
}

export default landing
